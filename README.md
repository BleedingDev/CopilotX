# CopilotX (archived)

This repository is archived and no longer the active home of CopilotX.

The maintained project now lives here:

- https://github.com/BleedingDev/effect-copilotx

Use the new repository for:

- active development
- issues and pull requests
- releases
- the Bun/Effect rewrite
- the dead-simple local startup flow

## Why this repo was archived

This repository mixed older implementation history with the in-progress rewrite.
The clean public cutover happened in `BleedingDev/effect-copilotx`, which is now
where the project is maintained.

## What to do instead

Go to the new repo and follow its README:

- `mise install`
- `mise run start`
- `mise run auth-login`

## Historical note

This repository remains available only for historical reference.
